#include <Stdio.h>
#include <stdlib.h>
#include "lzw.h"

void loadCharacters(char *string, LLNode *pList){
	for(int i=0;string[i] != '\0';i++){
		if( LL_Search_L(LLNode *pList, string[i]) )
			continue;
		else
			LL_add(&(*ppList), string[i] int n);
	}
}

